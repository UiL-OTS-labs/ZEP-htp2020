

// definitions in the experiment these are constant values used throughout
// the experiment.

const dur  LIGHT_ISI                    = 250ms;
const dur  LIGHT_DURATION               = 250ms;

// test/habituation phase.

const dur  TEST_ISI                     = 125ms;
const int  MAX_HABITUATION_TRIAL        = 25;
const int  MAX_TEST_TRIALS              = 2;        // Max number of test trials
                                                    // if <= 0 the number
                                                    // of test items.
const int  MAX_ATTENTION_TRIALS         = 1;        // max attention trials.
const real HABITUATION_CRITERION        = 65.0;
const dur  MAX_LOOK_AWAY_DURATION       = 2000ms;
const dur  CLOCK_UPDATE_PERIOD          = 20ms;

const int NUM_REPETITIONS 	            = 1;

const dur TEST_STIM_DURATION            = 15s;


const real CIRCLE_RADIUS                = 100;

const color FRONT_COLOR                 = rgbi:0/255/0;
const color SIDE_COLOR                  = rgbi:255/0/0;

enum SoundDirection {
    SND_DIRECTION_LEFT  = 1 << 0,
    SND_DIRECTION_RIGHT = 1 << 1,
    SND_DIRECTION_FRONT = 1 << 2,
    SND_DIRECTION_RIGHT_LEFT = SND_DIRECTION_LEFT | SND_DIRECTION_RIGHT

};

enum SoundChannel {
    CHANNEL_FRONT_LEFT  = 0,
    CHANNEL_FRONT_RIGHT = 1,
    CHANNEL_REAR_LEFT   = 2,
    CHANNEL_REAR_RIGHT  = 3
};

// A value between 0.0 and 1.0. 0.0 means off, 1.0 means maximum value.
const real ON_VOLUME  = 0.2;
const real OFF_VOLUME = 0.0;

